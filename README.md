# controle_mecanica
Sistema básico de controle de mecânica.

## Flask

Instale as dependências e inicie o servidor:

```powershell
py -m pip install -r requirements.txt
py app.py
```

Abra `http://127.0.0.1:5000`. Os registros são persistidos em SQLite no arquivo `data/oficina.db`.
